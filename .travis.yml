language: node_js
env:
  global:
    - NODE_PATH=$PWD/src
    - TZ=Canada/Eastern
before_install:
  - env | sort
  - npm install --global yarn
install:
  - make travis
node_js:
  - "node"
  - "lts/*"
script:
  - make test
